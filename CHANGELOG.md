# 変更履歴

## 2025-09-06

### 1. ディレクトリ名のタイポ修正

- `assets/scense` ディレクトリを `assets/scenes` にリネームしました。
- 関連する以下のファイル内の参照をすべて更新しました。
    - `GODOT3_MIGRATION.md`
    - `assets/codes/ControlUI.gd`
    - `assets/codes/Main.gd`

### 2. アルファベットを含む問題の不具合修正

- **問題:** `questions.json` の `kana` に "ACLゆうしょう" のようにアルファベットが含まれていると、パーサーが処理できずエラーになる。
- **修正:** `assets/codes/RomanTypingParser.gd` を修正し、ひらがな辞書にない文字（アルファベットなど）が来た場合でも、それをそのまま（小文字にして）タイプする文字として扱うようにしました。

### 3. 大文字・小文字の入力区別に対応

- **問題:** `Shift`キーを押しながら `A` を入力しても、小文字の `a` として扱われてしまい、大文字と小文字の区別ができなかった。
- **修正:**
    - `assets/codes/Main.gd` の入力処理を変更し、`Shift`キーの状態を判別して大文字・小文字を区別して受け取れるようにしました。
    - `assets/codes/RomanTypingParser.gd` の処理を再度修正し、問題文（`kana`）に含まれる大文字を大文字のままタイプするよう要求するようになりました。
